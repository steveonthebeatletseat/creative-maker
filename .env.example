# ============================================================
# Creative Maker — Environment Variables
# ============================================================
# Copy this file to .env and fill in your API keys:
#   cp .env.example .env
#
# At minimum, you need ONE provider key (whichever you want
# to use as your default). OpenAI is the default provider.
# ============================================================

# --- LLM Provider API Keys ---
# You need at least one. Get yours at:
#   OpenAI:    https://platform.openai.com/api-keys
#   Anthropic: https://console.anthropic.com/settings/keys
#   Google:    https://aistudio.google.com/apikey
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# --- Default Provider & Model ---
# Which provider to use when not overridden per-agent.
# Options: openai, anthropic, google
# DEFAULT_PROVIDER=openai
# DEFAULT_MODEL=gpt-5.2

# --- Output Directory ---
# Where agent outputs are saved (relative to project root).
# OUTPUT_DIR=outputs

# --- Logging ---
# LOG_LEVEL=INFO

# --- Per-Agent Overrides (optional) ---
# You can assign different providers/models to specific agents.
# Example: run Agent 1A on Anthropic while everything else uses OpenAI.
# AGENT_01A_PROVIDER=anthropic
# AGENT_01A_MODEL=claude-opus-4-20250514
# AGENT_01B_PROVIDER=openai
# AGENT_01B_MODEL=gpt-5.2
